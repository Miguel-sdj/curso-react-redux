import React from "react";

export default function Primeiro() {
  const MSG_BOAS_VINDAS = "Seja bem vindo(a) ao nosso aplicativo!";

  return (
    <div>
      <h2>Primeiro Componente</h2>
      <p>{MSG_BOAS_VINDAS}</p>
    </div>
  );
}
